Generate a keystore
-------------------

keytool -genkey -alias clientservice -keyalg RSA -keysize 2048 -keystore servicestore.jks -validity 3650

pwd: qwerty
first and last name: localhost (THIS IS IMPORTANT)
all other fields can be left to Unknown
